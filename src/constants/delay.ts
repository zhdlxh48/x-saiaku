// Delays
export const DEFAULT_SAFE_DELAY_MS: number = 5000;

export const DEFAULT_LIKE_DELAY_MIN_MS: number = 2500;
export const DEFAULT_LIKE_DELAY_MAX_MS: number = 4000;

export const DEFAULT_UNLIKES_DELAY_MIN_MS: number = 2500;
export const DEFAULT_UNLIKES_DELAY_MAX_MS: number = 4000;

export const DEFAULT_SCROLL_DELAY_MIN_MS: number = 1000;
export const DEFAULT_SCROLL_DELAY_MAX_MS: number = 2000;